#include <stdio.h>
int main()
{
    int a, b, i, flag;
   // printf("Enter two numbers(intevals): ");
    scanf("%d %d", &a, &b);
   if(a<b)
   {
    printf("%d is min",a);
   }
    else
    {
    printf("%d is min",b);
}
    return 0;
}
